function pow(x, y) {
  let result = 1;

  for (let i = 0; i < y; i++) {
    result = result * x;
  }

  return result;
}

// Приклад виклику:
console.log(pow(2, 3)); // 8
console.log(pow(5, 0)); // 1
console.log(pow(3, 4)); // 81
